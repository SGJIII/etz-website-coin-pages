{
  "name": "etz",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "parcel serve src/index.html --open --host localhost --port 1234",
    "build": "rimraf build && parcel build src/index.html --dist-dir build --public-url ./"
  },
  "devDependencies": {
    "posthtml-include": "^1.7.3",
    "parcel": "^2.4.1",
    "rimraf": "^3.0.2"
  }
}
